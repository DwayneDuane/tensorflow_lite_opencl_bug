Original Keras model and TFLite conversion script inside the `keras_model` folder.
`sample_outputs/cpu.txt` and `sample_outputs/gpu.txt` contain outputs from CPU and GPU delegate respectively on the Snapdragon 865.
The model used for this experiment is `dummy.tflite`.
